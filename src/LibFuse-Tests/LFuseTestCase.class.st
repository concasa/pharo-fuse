Class {
	#name : #LFuseTestCase,
	#superclass : #TestCase,
	#category : #'LibFuse-Tests-Tests'
}

{ #category : #running }
LFuseTestCase >> setUp [ 
	^ super setUp 
]

{ #category : #running }
LFuseTestCase >> tearDown [
	super tearDown.
]
